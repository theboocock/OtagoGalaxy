<tool id="compare_1kg" name="Compare VCF with 1000 Genomes" version ="1.0.0">
    <description>and filter by Allele Frequency</description>

    <command interpreter="bash">

    ./compare1kg.vcf

    </command>

    <inputs>
        <param name="inputVCF" label="VCF to compare" type="data" format="vcf" help="This is your vcf you wish to compare against 1000 Genomes"/>
        <param name="filterAF1" label="Filter your VCF by AF less than or equal to" type="text" help="Filter your VCF by Allele Frequencies less than or equal to some number" />
        <param name="select_by" type="select" display="checkboxes" multiple="True" optional="true" value ="" label="Select what to filter by" help="You can select all, just SNPs or just INDELs">
        <option value="snp">Filter just SNPs</option>
        <option value="indel">Filter just INDELs</option>
        <option value="both">Filter both</option>
        </param>
        <param name="filterAF2" label="Filter end VCF by AF less than or equal to" type="text" help="Filter end VCF by Allele Frequencies less than or equal to some number" />

    </inputs>

    <outputs>
        <data name="outputVCF" format="vcf" type="data" />
    </outputs>

    <help>
    This will search through the 1000 Genomes VCF and compare your vcf filtered by AF against it and then filter the result by AF as well.
    </help>
</tool>
